# 环境配置模板，复制为 env_test.yaml 后按实际填写
env_name: "test"

# 接口基础地址
base_url: "http://127.0.0.1:8080"

# 请求公共头
common_headers:
  Content-Type: "application/json"

# Mysql数据库配置
mysql:
  host: "127.0.0.1"
  port: 3306
  user: "root"
  password: "123456"
  database: "test_db"